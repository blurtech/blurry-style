import React from "react";
import styles from "./Input.module.scss";

interface INProps {
  disabeled?: boolean;
}

const Input = (props: INProps) => {
  return(
    <input/>
  )
}

export default Input