import React from "react";
import styles from "./NavigationBar.module.scss";

interface INProps {
  //we have probably send a number of pieces of content to this component in order to calc number of bar blocks
}

const NavigationBar = (props: INProps) => {
  return(
    <div>navigation</div>
  )
}

export default NavigationBar